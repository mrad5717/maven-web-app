apiVersion: apps/v1
kind: Deployment
metadata:
  name: maven-web-app-qa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: maven-web-app-qa
  template:
    metadata:
      labels:
        app: maven-web-app-qa
    spec:
      containers:
      - name: maven-web-app
        image: mururadh/maven-web-app:latest
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
